<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <android.support.v7.widget.CardView
            android:id="@+id/layout_profile"
            android:layout_width="@dimen/size_profile_big"
            android:layout_height="@dimen/size_profile_big"
            android:layout_centerHorizontal="true"
            app:cardCornerRadius="@dimen/size_profile_radius_big">

            <ImageView
                android:id="@+id/iv_profile"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/ic_placeholder_avatar" />
        </android.support.v7.widget.CardView>

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/layout_profile"
            android:layout_marginLeft="@dimen/spacing_smallest"
            android:layout_marginRight="@dimen/spacing_smallest"
            android:fontFamily="@font/cabin_bold"
            android:textColor="@color/colorTextPrimary"
            android:textSize="@dimen/text_size_normal"
            tools:text="Nguyen Hoang Phat" />
    </RelativeLayout>
</layout>